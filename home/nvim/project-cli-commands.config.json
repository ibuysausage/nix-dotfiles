{
  "commands": {
    "nixos": {
      "name": "NixOS rebuild",
      "description": "Run nixos-rebuild switch",
      "cmd": "sudo nixos-rebuild switch --flake /etc/nixos"
    },
    "update": {
      "name": "Nix flake update",
      "description": "Update nix flake",
      "cmd": "nix flake update --flake /etc/nixos"
    }
  }
}
